# Auto-generated by webapp/scripts/build_code_snippets.py
# session_id (+ experiment) come from POST /api/user_r/run (Code Lab).
# plumber.R clinical/reorient — plumber.R lines 244-250

b <- list(session_id = session_id, mode = "auto")
  p_legacy <- file.path(dirname(mae_path(session_id)), "clinical_uploaded.csv")
  write_standalone_clinical(meta, p_legacy)
}
merged_preview <- tryCatch(.clin_merge_external_tables(meta, paired_meta_upload), error = function(e) meta)
list(
  success = TRUE,
  session_id = session_id,
