# Auto-generated by webapp/scripts/build_code_snippets.py
# session_id (+ experiment) come from POST /api/user_r/run (Code Lab).
# metagenomics routes — bundled cores from plumber.R

if (!exists("experiment", inherits = FALSE) || is.null(experiment) || !nzchar(as.character(experiment)[1]))
  stop("experiment required: set in Code Lab or assign experiment <- 'YourExp'")



# --- POST /api/workflows/metagenomics/profile ---
b <- list(session_id = session_id, experiment = as.character(experiment)[1], id_type = "auto")
#* Transcriptomics WGCNA analysis
#* @post /api/workflows/transcriptomics/analyze/wgcna
#* @serializer unboxedJSON
function(req, res) {
  safe_api({


# --- POST /api/workflows/metagenomics/validate ---
b <- list(session_id = session_id, experiment = as.character(experiment)[1], id_type = "auto")
  method = method,
  cutoff = cutoff
)
result_df <- tryCatch(as.data.frame(result), error = function(e) data.frame())

list(


# --- POST /api/workflows/metagenomics/preprocess ---
b <- list(session_id = session_id, experiment = as.character(experiment)[1], max_na = 0.2, normalize_method = "rclr")
}

#* Metagenomics workflow profile (functional matrix checks)
#* @post /api/workflows/metagenomics/profile
#* @serializer unboxedJSON
function(req, res) {
  safe_api({


# --- POST /api/workflows/metagenomics/analyze/differential ---
b <- list(
  session_id = session_id, experiment = as.character(experiment)[1], id_type = "auto", method = "limma",
  group_var = NULL, ref_group = NULL, test_group = NULL
)

#* Metagenomics workflow pre-check validation
#* @post /api/workflows/metagenomics/validate
#* @serializer unboxedJSON
function(req, res) {
  safe_api({

    session_id <- b$session_id %||% NULL
    experiment <- b$experiment %||% NULL
    id_type <- b$id_type %||% "auto"
    list(success = TRUE, validation = mgx_validate(session_id, experiment, id_type))
  }, res)
}

#* Metagenomics preprocess with functional defaults
#* @post /api/workflows/metagenomics/preprocess
#* @serializer unboxedJSON
function(req, res) {
  safe_api({

    session_id <- b$session_id %||% NULL
    experiment <- b$experiment %||% NULL
    max_na <- b$max_na %||% 0.2
    normalize_method <- b$normalize_method %||% "rclr"

    mgx_preprocess(session_id, experiment, max_na, normalize_method)
  }, res)


# --- POST /api/workflows/metagenomics/analyze/enrichment ---
b <- list(session_id = session_id, experiment = as.character(experiment)[1], id_type = "auto", database = NULL, organism = "hsa")

session_id <- b$session_id %||% NULL
experiment <- b$experiment %||% NULL
id_type <- b$id_type %||% "auto"
method <- b$method %||% "limma"
group_var <- b$group_var %||% NULL
ref_group <- b$ref_group %||% NULL
test_group <- b$test_group %||% NULL

result <- mgx_run_differential(
  session_id = session_id,
  experiment = experiment,
  id_type = id_type,
  method = method,
  group_var = group_var,
  ref_group = ref_group,
  test_group = test_group
)
result_df <- tryCatch(as.data.frame(result), error = function(e) data.frame())

list(
  success = TRUE,
  n_rows = nrow(result_df),
